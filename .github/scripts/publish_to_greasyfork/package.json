{
  "name": "publish-to-greasyfork",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild main.ts --bundle --outdir=dist --platform=node --format=esm --sourcemap"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "22.2.0",
    "esbuild": "0.23.0"
  }
}
